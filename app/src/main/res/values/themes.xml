<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Beacon" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowLightStatusBar">@bool/is_light_status_bar</item>
    </style>

    <style name="Theme.Beacon.Dialog" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>